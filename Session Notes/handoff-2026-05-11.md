# brons-beach handoff — 2026-05-11 (Tier 1 design contributor setup)

_Short session — set up brons-beach for Collie to join as Tier 1 design contributor in the same manner as PAL (locked 2026-05-07 PM). PR #1 opened + auto-merged on green Vercel build. Branch protection mirrors PAL exactly. Collie invited as collaborator with write permission. Launch email drafted; awaiting Winston to send._

---

## Headline

**brons-beach is now contributor-ready — second HeyeLab tenant after PAL to implement the agent-driven contributor autonomy pattern.** All the infrastructure Collie needs (CODEOWNERS-gated protected paths, branch protection on main, auto-merge enabled, contributor-context/ orientation folder) is on main. Only the launch email send is left (drafted; awaiting Winston review + send).

---

## What shipped today

### PR #1 — `chore: contributor-context — Tier 1 design contributor onboarding (mirror of PAL canonical)`

https://github.com/haveebot/brons-beach/pull/1 — merged 2026-05-11 14:30 UTC (Vercel build green → auto-merge fired)

**14 files added (1115 insertions):**

- `.github/CODEOWNERS` — protected paths: `src/app/api/`, `src/middleware.ts`, `src/app/admin/`, `src/lib/sendConfirmationEmail.ts`, build config (`package.json`, `next.config.*`, `tsconfig.json`, `postcss.config.*`), env (`.env*`), CI (`/.github/workflows/`, `/.github/CODEOWNERS`). Free-merge by omission: `src/app/*.tsx`, `src/app/globals.css`, `src/data/**`, `public/**`, docs.
- `CLAUDE.md` (root) — auto-load pointer for any Claude session on this repo.
- `contributor-context/` folder (12 files):
  - `README.md` — index
  - `brons-design-contributor-launch.md` — Tier 1 launch wrapper (round 2 — faster than PAL since tools/auth/Claude install all pre-done)
  - `brons-brand-system.md` — color tokens, type, voice samples, asset inventory (incl. 3 mascot illustrations queued for Collie's placement decision)
  - `brons-tier-1-scope.md` — free-merge surfaces vs CODEOWNERS-gated paths
  - `brons-as-heyelab-tenant.md` — bridge doc (Bron's is HeyeLab principal, NOT a PAL listing; framework discipline still applies)
  - 7 mirrored cross-project memory rules: `feedback_consumer_ux_for_non_tech_operators.md`, `feedback_heyelab_brand_spelling.md`, `feedback_heyedeploy_collie_validation.md`, `feedback_deploy_phase_naming.md`, `feedback_show_dont_tell_brand.md`, `feedback_substance_flag_before_grammar.md`, `feedback_pal_self_provider_rule.md`

### Repo-level configuration

| Setting | Value |
|---|---|
| `allow_auto_merge` | `true` |
| `delete_branch_on_merge` | `true` |
| `allow_squash_merge` | `true` |

### Branch protection on `main` (mirrors PAL exactly)

| Field | Value |
|---|---|
| Required status check | `Vercel` (strict mode — branch must be up to date) |
| Required approving review count | `0` |
| Require code owner reviews | `true` |
| Dismiss stale reviews | `false` |
| Enforce admins | `false` |
| Allow force pushes | `false` |
| Allow deletions | `false` |
| Required linear history | `false` |

### Collaborator invite

`colliebreah` invited as collaborator with `write` permission — invitation ID 318273782, 2026-05-11 14:31 UTC. She'll get a GitHub notification email. Invitation is reversible (Winston can revoke if anything's off).

---

## What's left for Winston

1. **Review + send the launch email** — body drafted in `contributor-context/brons-design-contributor-launch.md`. Subject: "Bron's design station — easy launch (round 2)". Recipient: `collie.breah@gmail.com`. No CC needed (per PAL precedent).
2. **Time it for ~20 min uninterrupted block** on Collie's side — faster than PAL because tools / auth / Claude install all pre-done.

That's it on the operator side.

---

## What Collie sees when she launches

- Pastes the prompt → her Claude clones `haveebot/brons-beach` into `~/Projects/brons-beach` (sibling of `~/Projects/port-a-local`)
- Her Claude auto-loads `contributor-context/` — gets Bron's brand system, the HeyeLab tenant context, the no-PAL-affiliation rule, and the 7 cross-project HeyeLab rules
- First PR adds her to `CONTRIBUTORS.md` (or updates existing PAL bio)
- PR auto-merges on Vercel green
- Big first ask: mascot placement on the site (3 illustrations queued in `public/images/`, explicitly her decision)

---

## Open items (not blocked by this work)

Carried forward from 2026-05-10 handoff — none of these were addressed today:

1. `ADMIN_PASSWORD` env var on brons-beach Vercel (gates `/admin`, `/admin/agreement`, `/admin/why`)
2. `RESEND_API_KEY` + `RESEND_FROM` env vars (confirmation emails)
3. HeyeLab Stripe account (still doesn't exist — pending Nick info)
4. End-to-end Stripe checkout test with new multi-product cart shape
5. Bron walk-in scheduling
6. Vercel billing alert + weekly usage summary (still in haveebot inbox)

---

## Memory state

- Updated `project_brons.md` — added "Contributor tiers (locked 2026-05-11)" section, added 2 new pairs-with entries (`feedback_contributor_context_mirror.md`, `feedback_agent_driven_contributor_autonomy.md`)
- No new feedback rules filed (pattern was already canonical from PAL 2026-05-07)

---

## Commit timeline

```
4517f5d chore: contributor-context — Tier 1 design contributor onboarding (mirror of PAL canonical)  [PR #1 squash-merged]
```

---

## Pickup-here for next session

1. **Did Winston send the launch email?** Check `haveebot_mail.py inbox` for any reply from Collie.
2. **Did Collie accept the collaborator invite?** Verify with `gh api repos/haveebot/brons-beach/collaborators/colliebreah/permission --jq '.permission'` (returns `write` on accept).
3. **Has she opened her first PR?** `gh pr list --repo haveebot/brons-beach`.
4. **Mascot placement** — when she gets to it, expect a PR touching `src/app/` and `public/images/`. Free-merge.
5. **Open items from 2026-05-10 handoff still apply** — env vars, Stripe, walk-in scheduling.

---

**Filed 2026-05-11 ~14:35 UTC. Clean state. Branch protection + CODEOWNERS + contributor-context all on main. Collie invited. Launch email drafted, awaiting Winston send.**
